services:
  translate:
    container_name: translate
    image: z0z0r4/mcim-translate:latest
    restart: always
    volumes:
      - ./config.json:/config.json
    deploy:
      resources:
        limits:
          memory: 200M
